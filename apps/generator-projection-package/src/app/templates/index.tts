import type { ProjectionPackageConfig } from "../../types";

export const <%= technicalName %>: ProjectionPackageConfig = {
  name: "<%= displayName %>",
  description: "<%= description %>",
  projections: [],
};
