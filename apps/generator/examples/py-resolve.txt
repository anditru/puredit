filtered_students = students.select("age", first_name="name").to_pandas()

filtered_lectures = lectures.select("credits", professor="lecturer").to_pandas()

---

From students export columns
    age.
    name as first_name.
to Pandas into filtered_students.

From lectures export columns
    credits.
    lecturer as professor.
to Pandas into filtered_lectures.