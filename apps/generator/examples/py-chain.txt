students.select("name", "age", "semester").filter(age=24)

lectures.select("lecturer", "id", "credits").filter(credits=8)

---

    Transform dataframe students by.
    selecting columns name, age, semester.
    then filtering rows where age is 24.

    Transform dataframe lectures by.
    selecting columns lecturer, id, credits.
    then filtering rows where credits is 8.
